export enum State {
  FUTURE = 'future',
  ACTIVE = 'active',
  CLOSED = 'closed',
  RELEASED = 'released',
  UNRELEASED = 'unreleased',
}

export enum JiraFilterType {
  SPRINT = 'sprint',
  VERSION = 'version',
}
