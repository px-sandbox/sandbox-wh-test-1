name: StudioGraphene-SAST-Analytics
on:
  push:
    branches: [dev, master]
jobs:
  sast_analytics:
    uses: studiographene/github-action-workflows/.github/workflows/pulse-sast-analytics.yml@master
    secrets: inherit
