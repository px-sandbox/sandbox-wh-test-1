name: sg-dependacies-analytics

on:
  push:
    branches: [dev]
    
jobs:
  analytics:
    uses: studiographene/nodejs-ci/.github/workflows/dependencies-analytics.yml@feature/pulse-report
    secrets: inherit
    permissions: write-all
