name: CI checks

on:
  pull_request: {}
  workflow_dispatch:

jobs:
  call-workflow:
    uses: studiographene/studiographene-reactjs-scans/.github/workflows/ci.yml@feature/pnpm
    with:
      project_name: px-data-integration
